//make sure that before accessing any array, we check the lenght first:


fun lenCheckBeforeArrayAccess(tr: TranslationResult) : QueryTree<Boolean> {

    return TODO()
}

fun noUseAfterFree(tr: TranslationResult) : QueryTree<Boolean> {
    return TODO()
}